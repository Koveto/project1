b.player_team[b.turn_index].is_guarding = False

STATE_MAIN -> STATE_SKILLS
STATE_SKILLS -> STATE_SKILLS_TARGET_ENEMY
STATE_SKILLS_TARGET_ENEMY -> stack...
stack...STATE_SCROLL
        STATE_WAIT
        STATE_CALC_PLAYER_DMG_TO_ENEMY 
miss! STATE_CALC_PLAYER_DMG_TO_ENEMY -> STATE_SCROLL -> end
hit!  STATE_CALC_PLAYER_DMG_TO_ENEMY
      STATE_WAIT
      STATE_WAIT
      STATE_ANIMATE_HP_PLAYER_SINGLE_TARGET
      STATE_SCROLL